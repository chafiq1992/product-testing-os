# Node
node_modules
.next
.vercel
.turbo
pnpm-lock.yaml

# Python
.venv
venv
__pycache__/
*.py[cod]

# OS
.DS_Store

# Env & build
.env
.env.*
!.env.example
dist
build
uploads

# Expo app with its own toolchain. android/keystore.properties and keystore/
# are release-signing material and must never enter git history.
mobile-app/

# Local agent worktrees: full copies of this repo.
.claude/

# Scratch output
artifacts/
*.db-journal
